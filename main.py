name = input("What is your name? " )
# Add the sep command to this print so the ! is right beside the name
print("Hello ",name,"!")

#Modify this line so that pennies is an integer instead of a string
money = input("Pennies do you want? ")
money = money/100
#Add a format command to the print so that it prints the money with two decimals 
#Also add a sep command so the $ is right beside the money
print("You want $", money)
